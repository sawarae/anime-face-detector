# Lightweight ONNX-only version
numpy>=1.21.3
opencv-python-headless>=4.5.4.58
onnx>=1.10.0
onnxruntime-gpu>=1.10.0; platform_system != "Darwin"
onnxruntime>=1.10.0; platform_system == "Darwin"

# Optional: Install PyTorch dependencies for model conversion
# Uncomment if you need to convert models from PyTorch to ONNX
# mmcv-full>=1.3.16
# mmdet>=2.18.0
# mmpose>=0.20.0
# torch>=1.10.0
# torchvision>=0.11.1
